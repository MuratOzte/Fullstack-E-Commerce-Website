interface filterData {
    minPrice: number;
    maxPrice: number;
    minStar: number;
    battery: number;
    storage: number;
    ram: number;
}

const filterByProps = (url: string, data: filterData) => {
    
};
