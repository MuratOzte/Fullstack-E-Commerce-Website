import LoginContainer from '@/components/acount/LoginContainer';

const Account = () => {
    return (
        <div
            style={{
                display: 'flex',
                color: 'black',
            }}
        >
            <LoginContainer />
        </div>
    );
};

export default Account;
